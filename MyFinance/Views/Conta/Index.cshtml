
<h3>Minhas contas</h3>

<table class="table table-bordered">
    <thead>
        <tr>
            <th>Nome</th>
            <th>Saldo</th>
            <th>Ações</th>
        </tr>
    </thead>
    @{
        foreach (var item in (List<ContaModel>)ViewBag.ListaConta)
        {
            <tbody>
                <tr>
                    <td>@item.Nome</td>
                    <td>R$ @item.Saldo?.ToString("N2")</td>
                    <td>
                        <button type="button" class="btn btn-danger" onclick="Excluir(@item.Id.ToString())">Excluir</button>
                        <button type="button" class="btn btn-primary" onclick="Editar(@item.Id.ToString())">Editar</button>
                    </td>
                </tr>
            </tbody>
        }
    }
</table>

<button type="button" class="btn btn-block btn-success" onclick="Inserir()">Criar Conta</button>

<script>

    function Inserir() {
        window.location.href = "../Conta/CriarConta"
    }

    function Excluir(id) {
        window.location.href = "../Conta/ExcluirConta/" + id;
    }
    function Editar(id) {
        window.location.href = "../Conta/CriarConta/" + id;
    }
</script>